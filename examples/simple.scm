;; ignore this comment

(define x 1)

(display 'hello 'world!)

(let ((y 20))
    ;; and this comment
    ((lambda (x y z)
      ; and this
        (+ x y z))  ; and this
        ; this also
     300 y x))

;; and this comment as well
